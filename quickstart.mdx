---
title: 'Quickstart'
description: 'Get started with Incredible API in minutes'
---

Welcome to **Incredible**! This guide will get you up and running in under 3 minutes.

## 🚀 Step 1: Make Your First API Call

The Incredible API works without authentication for basic usage. Here's your first request:

```bash
curl -X POST "https://api.incredible.one/v1/chat-completion" \
  -H "Content-Type: application/json" \
  -d '{
    "model": "small-1",
    "messages": [
      { "role": "user", "content": "Hello, Incredible!" }
    ]
  }'
```

**Response:**
```json
{
  "result": {
    "thinking": "The user is greeting me...",
    "response": [
      {
        "content": "Hello! I'm here to help you...",
        "role": "assistant"
      }
    ]
  }
}
```

## 🔄 Step 2: Try Streaming

Enable streaming for real-time responses:

```bash
curl -X POST "https://api.incredible.one/v1/chat-completion" \
  -H "Content-Type: application/json" \
  -d '{
    "model": "small-1",
    "stream": true,
    "messages": [
      { "role": "user", "content": "Count to 3" }
    ]
  }'
```

## 🛠️ Step 3: Test Function Calling

Try a simple calculation:

```bash
curl -X POST "https://api.incredible.one/v1/chat-completion" \
  -H "Content-Type: application/json" \
  -d '{
    "model": "small-1",
    "messages": [
      { "role": "user", "content": "What is 15 + 27?" }
    ],
    "functions": [{
      "name": "add_numbers",
      "description": "Add two numbers",
      "parameters": {
        "type": "object",
        "properties": {
          "a": {"type": "number"},
          "b": {"type": "number"}
        },
        "required": ["a", "b"]
      }
    }]
  }'
```

## 🎯 What's Next?

<div style={{ display: 'grid', gridTemplateColumns: 'repeat(3, minmax(0, 1fr))', gap: '16px' }}>
  <Card title="Core Concepts" icon="brain" href="/guides/core-concepts">
    Understand what makes Incredible different.
  </Card>
  <Card title="Function Calling" icon="wrench" href="/guides/function-calling">
    Learn to use custom functions.
  </Card>
  <Card title="Examples" icon="file-code" href="/examples/hello-incredible">
    See more examples in action.
  </Card>
</div>

<div style={{ border: '1px solid #FF6200', borderRadius: '12px', padding: '16px', margin: '16px 0', background: 'rgba(255, 98, 0, 0.08)' }}>
  <strong>Need Help?</strong> Join our <a href="https://discord.com/invite/pNmdkxgBe2" style={{color: '#FF6200'}}>Discord community</a> or check the <a href="https://github.com/Norditech-AB/Incredible-API-Cookbook" style={{color: '#FF6200'}}>GitHub cookbook</a>.
</div>

